<div class="wrapper">
  <h1>eva·bot</h1>

  <%= form_for(:session, url: session_path) do |f| %>
    <%= f.text_field :token,
      placeholder: "access token",
      autocorrect: "off",
      autofocus: true
    %>
  <% end %>
</div>
